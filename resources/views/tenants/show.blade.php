@extends('layouts.admin')
@section('title', $tenant->user->name)
@section('content')
<div class="mb-6 flex items-center justify-between"><div><a href="{{ route('tenants.index') }}" class="text-sm font-semibold text-[#12347f]">← Data Penyewa</a><h1 class="mt-3 text-3xl font-extrabold">{{ $tenant->user->name }}</h1></div><a href="{{ route('tenants.edit', $tenant) }}" class="btn-primary"><x-icon name="edit"/> Edit</a></div><section class="panel max-w-4xl p-7"><div class="grid gap-8 md:grid-cols-2"><div><h2 class="font-extrabold">Informasi Kontak</h2><dl class="mt-5 space-y-4 text-sm"><div><dt class="text-slate-500">Email</dt><dd class="font-semibold">{{ $tenant->user->email }}</dd></div><div><dt class="text-slate-500">Nomor HP</dt><dd class="font-semibold">{{ $tenant->user->phone ?: '-' }}</dd></div><div><dt class="text-slate-500">Nomor KTP</dt><dd class="font-semibold">{{ $tenant->identity_number ?: '-' }}</dd></div></dl></div><div><h2 class="font-extrabold">Informasi Sewa</h2><dl class="mt-5 space-y-4 text-sm"><div><dt class="text-slate-500">Properti</dt><dd class="font-semibold">{{ $tenant->room->kost->name }}</dd></div><div><dt class="text-slate-500">Kamar</dt><dd class="font-semibold">{{ $tenant->room->room_number }}</dd></div><div><dt class="text-slate-500">Check-in</dt><dd class="font-semibold">{{ $tenant->check_in->format('d M Y') }}</dd></div></dl></div></div></section>
@endsection
