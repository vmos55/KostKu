@extends('layouts.admin')
@php $editing = isset($room); @endphp
@section('title', $editing ? 'Edit Kamar' : 'Tambah Kamar')
@section('content')
<div class="mb-6"><a href="{{ route('rooms.index') }}" class="text-sm font-semibold text-[#12347f]">← Kembali ke Data Kamar</a><h1 class="mt-3 text-3xl font-extrabold">{{ $editing ? 'Edit Kamar' : 'Tambah Kamar Baru' }}</h1></div>
<form method="POST" action="{{ $editing ? route('rooms.update', $room) : route('rooms.store') }}" class="panel max-w-3xl p-7">@csrf @if($editing) @method('PUT') @endif<div class="grid gap-6 md:grid-cols-2"><div><label class="form-label">Properti</label><select name="kost_id" class="form-select" required><option value="">Pilih Kost</option>@foreach($kosts as $kost)<option value="{{ $kost->id }}" @selected(old('kost_id', $room->kost_id ?? '') == $kost->id)>{{ $kost->name }}</option>@endforeach</select></div><div><label class="form-label">Nomor Kamar</label><input name="room_number" class="form-input" value="{{ old('room_number', $room->room_number ?? '') }}" required placeholder="A-01"></div><div><label class="form-label">Lantai</label><input type="number" name="floor" class="form-input" value="{{ old('floor', $room->floor ?? '') }}" min="1"></div><div><label class="form-label">Harga per Bulan</label><input type="number" name="price" class="form-input" value="{{ old('price', $room->price ?? '') }}" min="0" required></div><div><label class="form-label">Status</label><select name="status" class="form-select"><option value="available" @selected(old('status', $room->status->value ?? 'available') === 'available')>Tersedia</option><option value="reserved" @selected(old('status', $room->status->value ?? '') === 'reserved')>Dipesan</option><option value="occupied" @selected(old('status', $room->status->value ?? '') === 'occupied')>Terisi</option></select></div></div><div class="mt-8 flex justify-end gap-3"><a href="{{ route('rooms.index') }}" class="btn-secondary">Batal</a><button class="btn-primary">Simpan Kamar</button></div></form>
@endsection
